import React, { useState } from 'react';
import { DisclaimerView } from './DisclaimerView';

export const ConsentModal: React.FC = () => {
  return null; // DisclaimerView is rendered as a top-level stage in App.tsx
};

export { DisclaimerView };
